```mermaid
sequenceDiagram
    participant user
    participant browser
    participant server

    user->>browser: Writes note and clicks Save
    Note right of browser: JavaScript handles the event

    browser->>server: POST /new_note_spa (note as JSON)
    activate server
    Note right of server: Server saves the note
    server-->>browser: JSON response (created note)
    deactivate server

    Note right of browser: JavaScript updates the UI with new note
